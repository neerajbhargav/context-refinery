{
  "identifier": "default",
  "description": "Default capability set for ContextRefinery",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:default",
    "shell:allow-spawn",
    "shell:allow-execute",
    "dialog:allow-open",
    "dialog:default",
    "fs:allow-read-text-file",
    "fs:default"
  ]
}
